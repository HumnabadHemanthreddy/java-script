function checkFreezing(temp) {
    if (temp <= 0) {
    console.log("The temperature is at or below freezing point.");
    } else {
    console.log("The temperature is above freezing point.");
    }
}

checkFreezing(-5);//The temperature is at or below freezing point.celsius_fun.js:3
checkFreezing(10);//The temperature is above freezing point.